#!/bin/bash
#
# Use sudo to kill all children of "my" process group.

pgroup=$(ps -ho pgid $$)
members=$( ps  --sid $pgroup -ho pid)
children=${members/$$/}
if [ -n "${children/ /}" ]; then
    # Children might be gone already (#13).
    kill -TERM $children  &> /dev/null || :
fi
